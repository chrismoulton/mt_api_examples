Copyright 2010, MediaTemple, Inc.

This package contains example clients and libraries for accessing the Media Temple Public API.

Directory Layout

* lib - libraries for integrating with the API in your own software
  * js - a JavaScript client library with HTML harness that uses it

client - clients for users to interact with the API
  * shell - a shell script client that uses curl to interact with the (mt) Public API


IMPORTANT

There is a configuration file "mtapi.conf" that contains important configuration info that
DOES need to be updated before the examples will work.
(specifically, the API_KEY setting needs to be set)
